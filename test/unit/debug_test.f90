#include "test_macro.fi"

program debug_test
  ! use, intrinsic :: iso_fortran_env
  ! use iso_c_binding
  ! use test_mod
  ! use fazang
  ! implicit none

  ! type(var) a(3), b, c(3), d(3)
  ! a = var([1.d0, 2.d0, 3.d0])
  ! b = var(0.5d0)
  ! c = 2.d0 * a
  ! d = log(b * a * exp(c))

  ! call d(2)%grad()

end program debug_test
